#include <stdio.h>
/*Now to the main code main- serves as the entry point for the program
/* code function also does the same */
int main(void)
{puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
